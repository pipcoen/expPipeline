function psychoCurve(models2fit, crossVal, maxAndMin)
if ~exist('models2fit', 'var'); models2fit = {; end
if ~exist('crossVal', 'var'); crossVal = 5; end
if ~exist('maxAndMin', 'var'); maxAndMin = 1; end
    
    
end
